/** @type {import('next').NextConfig} */
//const nextConfig = {};
const nextConfig = {
    reactStrictMode: false,
    images : {
      domains : ['localhost', '127.0.0.1'] // == Domain name
    }
  }
  export default nextConfig;